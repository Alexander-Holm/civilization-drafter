<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Civilization_draft">

    

    <Style x:Key="CivButton" TargetType="{x:Type CheckBox}">
        <Style.Resources>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="TextAlignment" Value="Left"/>
            </Style>
            
            <!--Styles the dlc text-->
            <Style x:Key="Dlc" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="Bold"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Dlc}" Value="R&amp;F">
                        <Setter Property="Foreground" Value="Brown"/>
                        <Setter Property="FontFamily" Value="Allegro"/>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Dlc}" Value="GS">
                        <Setter Property="Foreground" Value="Blue"/>
                        <Setter Property="FontFamily" Value="Comic Sans MS"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>            
        </Style.Resources>
        
        <Setter Property="IsChecked" Value="{Binding IsChecked}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">

                    <!--Determines the overall look of the button-->
                    <Border x:Name="WholeButton"
                            DataContext="{Binding Civ}"
                            BorderThickness="1.5" 
                            BorderBrush="Black"
                            CornerRadius="8"
                            Margin="4,2">
                        <Border.Background>
                            <SolidColorBrush Color="AliceBlue" Opacity="0.7"/>
                        </Border.Background>
                        <DockPanel Width="155">

                            <!--Content of the button-->
                            <Border BorderThickness="2"
                                    CornerRadius="2"
                                    BorderBrush="Black" 
                                    Margin="6"
                                    Width="25"
                                    HorizontalAlignment="Left" VerticalAlignment="Center">
                                <Image Source="{Binding Image}"/>
                            </Border>
                            <StackPanel>
                                <DockPanel>
                                    <TextBlock Text="{Binding Name}"                                               
                                               FontSize="14"
                                               FontWeight="Bold"
                                               Margin="0,2,0,0"/>
                                    <TextBlock Style="{StaticResource Dlc}"
                                               Text="{Binding Dlc}" 
                                               VerticalAlignment="Top" 
                                               HorizontalAlignment="Right"
                                               Margin="0,4,5,0"/>
                                </DockPanel>
                                <TextBlock Text="{Binding Leader}"
                                           Margin="0,0,0,3"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>                    
                    
                    <ControlTemplate.Triggers>
                        <!--When the button is unchecked-->
                        <Trigger Property="IsChecked" Value="False">
                            <Setter TargetName="WholeButton" Property="Border.Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="Gray" Opacity="0.4"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>